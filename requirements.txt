# Runtime requirements for the pipeline and the dashboard.
#
# The desktop agent's dependencies (pynput, plyer) are deliberately NOT here:
# they are desktop-only, and a headless deployment neither needs nor can use
# them. Install them with:  pip install -e ".[agent]"

# --- core: data, pipeline, models, figures ---------------------------------
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0
joblib>=1.3
cryptography>=41.0
pyarrow>=14.0

# --- layer 5: dashboard ----------------------------------------------------
streamlit>=1.30
altair>=5.0
